import { FinanceRecord, LedgerEntry, UniversityPayout, Commission } from '../types/finance';

// All mock data removed — finance is now strictly handled via Supabase

export const mockFinanceRecords: FinanceRecord[] = [];
export const mockUniversityPayouts: UniversityPayout[] = [];
export const mockPartnerCommissions: Commission[] = [];
export const mockLedger: LedgerEntry[] = [];
